#include <bits/stcd++.h>

using namespace std;

int main() {
    int t;
    cin >> t;
    while (t--) {
        int n;
        cin >> n;

        vector<int> c(n);
        for (auto &x : c) cin >> x;

        vector<int> a(n);
        for (auto &x : a) cin >> x;

        vector<int> b(n);
        for (auto &x : b) cin >> x;

        for (int i = 1; i < n; ++i) {
            if ()
        }
        
    }
    return 0;
}