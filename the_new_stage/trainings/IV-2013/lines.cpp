using namespace std;
#include <algorithm>
#include <iostream>
#include <iterator>
#include <sstream>
#include <fstream>
#include <cassert>
#include <climits>
#include <cstdlib>
#include <cstring>
#include <string>
#include <cstdio>
#include <vector>
#include <cmath>
#include <queue>
#include <deque>
#include <stack>
#include <list>
#include <map>
#include <set>

int L,C;

// vector<vector<int> > state_b;

struct state{
  // vector<vector<int> > state_b;
  // int ** arr;

  // state(const int L,const int C){
  //   int **matriz = (int**) malloc(L*sizeof(int*));
  //   for (int i = 0; i < L; i++){
  //     (matriz)[i] = (int*) malloc(C*sizeof(int));
  //   }
  //   arr = matriz;
  // }

};

int main(){
  // make_matriz_2d(&arr, 2);
  // state(2,2);
  // while( cin >> L >> C ){
  //   for( int i = 0; i < L; ++i ){
  //     for( int j = 0; j < C; ++j ){
  //       cin >> i >> j;
  //       
  //     }
  //   }
  // }
  
  return 0;
}